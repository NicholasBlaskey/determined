:orphan:

**Improvements**

-  CLI: ``det user login <username>`` can now take a parameter ``--password-from-stdin`` to allow
   for easier use of the Determined CLI in scripts. Example usage ``cat passwordfile.txt | det user
   login admin``.
